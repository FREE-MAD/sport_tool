# sport_tool

体育招生考试评分工具项目，当前主线包含：

- 评分表数据整理与结构化
- 微信小程序查分页面
- 微信云函数查表评分
- 本地测试环境与线上正式环境区分

---

## 本轮重点更新说明

本次 README 重点补充以下 5 个方向，并把目前已经落地的实现细节写清楚：

1. 数据结构重构
2. 静默登录
3. 湖北数据
4. 提交方法封装
5. 防重复提交

---

## 1. 数据结构重构

这轮重构的核心目标，是把“评分规则”“省份开关”“数据库查表结构”三件事彻底拆开，避免继续把所有逻辑堆在一个大文件里，后续新增省份时也更容易扩展。

### 1.1 前端规则从单文件改为分省加载

原先规则主要集中在 `miniprogram/pages/tool/tool_f/rule2/rules.js`，这个文件现在保留为开发参考词典，不再作为运行时唯一入口。

当前运行时改为：

- `miniprogram/pages/tool/tool_f/rule2/distinguish_provinces_rule/index.js`
- `miniprogram/pages/tool/tool_f/rule2/distinguish_provinces_rule/001rule.js`
- `miniprogram/pages/tool/tool_f/rule2/distinguish_provinces_rule/002rule.js`
- `miniprogram/pages/tool/tool_f/rule2/provinceMap.js`

职责拆分如下：

- `provinceMap.js`
  - 只负责定义“这个省当前开放哪些大类/子类”
  - 控制项目是否启用、显示顺序、默认项
- `001rule.js`、`002rule.js`
  - 只负责该省份自己的详细规则
  - 包含主项、专项、辅项、分组、并行输入、计量单位、评分方向等定义
- `distinguish_provinces_rule/index.js`
  - 统一导出所有已接入省份规则
- `tool_f.js`
  - 根据当前所选省份动态加载规则，不再依赖一个全局大而全的规则对象

这样做之后，新增一个新省份时，理论上只需要补两层：

1. 在 `distinguish_provinces_rule/` 下新增对应省份规则文件
2. 在 `provinceMap.js` 中登记当前省份开放哪些项目

不需要再去全局大文件里到处改判断分支。

### 1.2 湖南规则支持二级分类和辅项

这次重构不只是“拆文件”，也是为了适配湖南这类更复杂的业务结构。

目前湖南已经支持：

- 主项编码，如 `1A`
- 专项编码，如 `1a / 2b / 3c / 4d / 5e`
- 辅项编码，如 `1aa / 2bb`
- 同一类型下的分组切换
- 专项与辅项合并提交到云函数统一评分

也就是说，现在前端已经不是单纯的“一个主项 + 一个专项”的固定结构，而是可以根据省份配置，动态生成表单结构。

### 1.3 数据库评分表改为“按 code 直查”

云函数 `cloudfunctions/sport_tool/sport_tool_fun1/index.js` 目前已经按扁平文档结构查表：

- 一条文档对应一个 `code`
- 通过 `where({ code })` 直接查询
- 单次请求内使用缓存，避免重复查同一条评分表

当前约定是：

- `tool_code`：正式环境评分集合
- `test_tool_code`：本地开发测试集合

这意味着数据库结构也从“嵌套式找表”逐步转向“扁平式按编码定位”，后面维护会轻很多。

### 1.4 不分性别编码也纳入统一规则层

部分项目数据库本身不分男女，因此前端不能继续拼出 `m` 或 `f` 编码，而要改成 `o`。

现在规则层支持通过 `genderless: true` 明确声明该项目为不分性别项目。当前已明确接入的例子包括：

- 广东排球：传球、垫球
- 广东乒乓球：左推右攻
- 湖南部分专项项目

这样前端编码生成时会自动把性别位修正为 `o`，避免查库命中不到数据。

---

## 2. 静默登录

本轮已经补上小程序启动阶段的静默登录，目标是让查分页面在真正提交前，就已经具备用户身份信息，而不是等用户点提交时再临时兜底。

### 2.1 启动时自动获取 openid

入口位于 `miniprogram/app.js`。

小程序启动后会：

1. 初始化云环境
2. 调用云函数 `login`
3. 获取并缓存：
   - `openid`
   - `appid`
   - `unionid`
   - `runtimeEnvVersion`
   - `loginUserCollectionName`

这一步对用户是无感的，不需要额外点击登录按钮，所以这里记为“静默登录”。

### 2.2 登录云函数同步写入用户记录

对应云函数位于：

- `cloudfunctions/sport_tool/login/index.js`

当前逻辑不是只返回 `openid` 就结束，而是会根据环境自动写入用户集合：

- 开发环境：`test_tool_loginuser`
- 正式环境：`realenvironment_tool_loginuser`

并且按 `openid` 做 upsert：

- 已有用户：更新时间
- 新用户：创建新记录

这样后续如果要做“历史记录”“用户行为统计”“按用户查提交记录”，基础数据已经有了。

### 2.3 提交前仍保留兜底登录能力

虽然 `app.js` 已经会在启动时拿一次 `openid`，但为了避免以下情况：

- 小程序刚启动时用户网络不稳
- 首次调用登录云函数失败
- `globalData` 丢失
- 本地缓存缺失

`miniprogram/utils/globalSubmit.js` 里仍然保留了 `ensureOpenId()` 兜底逻辑。

也就是说，现在是“双保险”：

- 启动时先静默登录
- 真正提交时再确认一次 `openid` 是否可用

这样能明显减少“用户已经填完表单，最后因为身份信息没拿到而提交失败”的情况。

---

## 3. 湖北的数据

这里单独说明一下当前状态，避免后面看 README 时误以为湖北已经前端上线。

### 3.1 当前湖北还没有正式接入运行时规则

目前 `distinguish_provinces_rule/index.js` 实际只导出了两个省份：

- `001` 广东
- `002` 湖南

同时 `provinceMap.js` 当前也只配置了：

- 广东省
- 湖南省

也就是说，从小程序运行时角度看，湖北目前还没有正式挂到省份选择和动态规则加载流程里。

### 3.2 湖北数据应放在哪一层

如果后续接入湖北，建议继续沿用这次重构后的分层方式：

1. 新增省份规则文件，例如 `003rule.js`
2. 在 `distinguish_provinces_rule/index.js` 注册导出
3. 在 `provinceMap.js` 增加湖北的项目启用配置
4. 数据库中准备湖北对应的扁平评分表文档，并保证每条文档存在唯一 `code`

这套方式能保证湖北不会再走回“写死在全局 rules.js 里”的老路。

### 3.3 湖北接入前需要确认的关键点

湖北数据正式接入前，至少要先确认以下内容：

- 主项、专项、辅项是否存在多层级编码
- 是否有不分性别项目
- 是否存在时间制项目
- 是否存在 `rule: false` 的直接给分项目
- 数据库 `code` 编码规则是否与广东/湖南同构
- 原始评分表是否已经按 `value` 与 `score` 对齐完成

如果这些前置不统一，前端虽然能先把省份按钮做出来，但提交到云函数后仍然会因为编码或数据结构不一致而查不到结果。

### 3.4 当前建议

湖北这部分现在更适合定义为：

- 数据整理/接入准备中
- 规则接入位已由本次架构重构预留完成
- 真正上线前只差规则文件、启用配置和数据库评分表三部分补齐

---

## 4. 提交方法封装

这次一个很重要的优化，是把“小程序提交云函数”这件事从业务页面里抽出来，统一封装到了：

- `miniprogram/utils/globalSubmit.js`

这不是简单的代码搬家，而是把提交流程标准化了。

### 4.1 统一解决了哪些问题

封装之后，一个页面发起云函数提交时，可以统一处理：

- 自动识别当前运行环境
- 自动补齐 `openid`
- 自动生成请求时间
- 自动生成客户端请求 ID
- 自动记录页面路由
- 自动生成提交指纹
- 自动做本地防重复提交
- 自动打印提交流转日志

这样业务页只需要关心两件事：

1. 我要调哪个云函数
2. 我要提交什么业务数据

其它通用问题都交给 `globalSubmit.js`。

### 4.2 当前 `tool_f.js` 的提交流程

在 `miniprogram/pages/tool/tool_f/tool_f.js` 中，页面在校验通过后，会先整理出：

- `mainData`
- `specialDataList`
- `auxiliaryDataList`
- `runtimeEnvVersion`

然后统一调用：

```js
submitCloudForm({
  name: 'sport_tool_fun1',
  data: callData,
  runtimeEnvVersion,
  lockKey: 'tool_f_submit',
  duplicateWindowMs: 5000,
  businessName: '体育查分表单提交',
  submitPageRoute: '/pages/tool/tool_f/tool_f',
  returnPageRoute: '/pages/tool/tool_f/tool_f_detailed/tool_f_detailed'
})
```

这意味着业务页面已经不再直接裸调 `wx.cloud.callFunction`。

### 4.3 封装后数据会自动附带提交元信息

`submitCloudForm()` 会在原始业务参数之外，再自动补一个 `__submitMeta`，当前包含：

- `clientSubmitAt`
- `clientSubmitTimestamp`
- `clientRequestId`
- `submitPageRoute`
- `submitLockKey`
- `submitFingerprint`
- `duplicateWindowMs`

云函数收到这些信息后，就能知道：

- 这次请求是谁发的
- 从哪个页面发起
- 大概在什么时间发起
- 是否与上一条请求内容完全相同

这对后续排查“重复提交”“页面跳转异常”“线上偶发失败”非常有用。

### 4.4 统一环境切换

`globalSubmit.js` 也统一封装了 `getRuntimeEnvVersion()`。

当前环境规则为：

- `develop` -> 查询测试集合
- 其它环境 -> 查询正式集合

对应云函数 `sport_tool_fun1` 会自动映射到：

- `test_tool_code`
- `tool_code`

这样做之后，前端页面本身不需要再手动区分“这次到底该查测试库还是正式库”。

---

## 5. 防重复提交

这部分这轮也专门做了收口，因为查分类场景很容易出现：

- 用户连续点两次按钮
- 页面还在 loading，用户以为没反应继续点
- 同一份内容短时间反复提交

如果不拦住，就会造成：

- 云函数重复执行
- `tool_records` 写入重复记录
- 用户看到多次结果，不知道哪次是有效的

### 5.1 前端正在执行时加锁

`globalSubmit.js` 内部使用 `inFlightSubmitMap` 做“提交中锁”。

逻辑是：

- 同一个 `lockKey` 正在执行时
- 后续再次点击会直接抛出 `DUPLICATE_SUBMIT`
- 页面提示“请勿重复提交”

同时，`tool_f.js` 页面也有自己的 `submitting` 状态，按钮会进入 loading 并禁用。

所以现在是两层保护：

- 页面按钮禁点
- 提交工具层再次加锁

### 5.2 相同内容在保护窗口内再次提交会被拦截

除了“同一时刻不能并发提交”，还增加了“短时间内相同内容不能重复提交”。

`submitCloudForm()` 会基于以下信息生成 `submitFingerprint`：

- 云函数名
- 业务名
- 提交页
- 返回页
- 环境
- openid
- 业务数据本身

然后结合 `recentCompletedSubmitMap` 记录最近一次成功提交时间。

当前默认保护窗口是：

- `5000ms`

也就是同一个用户、同一份内容、同一路由，在 5 秒内再次提交，会被本地直接拦截，不再打到云函数。

### 5.3 服务端防重目前先停用

云函数 `sport_tool_fun1` 里保留了 `findRecentDuplicateRecord()` 这个入口，但当前实现明确返回 `null`，也就是：

- 现在主防重在前端
- 服务端二次校验逻辑暂时停用

这样处理的原因也很现实：

- 先把前端重复点击这个高频问题压住
- 避免服务端“误判重复”影响正常提交
- 后面如果线上观察到还有重复写入，再恢复基于 `openid + submitFingerprint` 的服务端兜底方案

### 5.4 记录层已经预留了防重排查字段

虽然服务端硬拦截暂时停用，但写入 `tool_records` 时已经把这些字段落库了：

- `clientRequestId`
- `clientSubmitAt`
- `clientSubmitTimestamp`
- `submitPageRoute`
- `submitLockKey`
- `submitFingerprint`
- `duplicateWindowMs`
- `runtimeEnvVersion`

所以后面如果你要排查“到底有没有重复提交”“同一用户是不是连续点了多次”“开发环境和正式环境有没有串数据”，已经有基础数据可查。

---

## 6. 7.22 今日更新

今天这一轮的重点不再只是单点修补，而是把湖南规则、小程序提交链路、2023 结构化数据修复、2026 新规归总以及游泳专项重建几条线同步收口。相比 7.17 更偏架构稳定化，这一轮更偏“把关键数据修准、把规则链路打通、把归总口径纠正”。

### 6.1 湖南专项规则与小程序逻辑修正

这部分主要围绕湖南专项规则编码和前端联动展开，目标是让规则定义、页面录入和提交到云函数的编码结构真正一致，不再出现“规则文件能配、页面不能提、提交后查不到”的断层。

本轮已经完成：

- 依据《湖南数据编码和规则.json》重写湖南专项编码结构
- 补全篮球等项目的 `techSkill` 附加评审项逻辑
- 支持 `5ej` 这类分组型专项编码
- 同步调整以下文件的联动逻辑：
  - `miniprogram/pages/tool/tool_f/rule2/distinguish_provinces_rule/002rule.js`
  - `miniprogram/pages/tool/tool_f/tool_f_rules.js`
  - `miniprogram/pages/tool/tool_f/tool_f_submit.js`
  - `miniprogram/pages/tool/tool_f/tool_f.js`
  - `miniprogram/pages/tool/tool_f/tool_f.wxml`
- 前端已新增附加评审分输入能力
- 当前湖南专项启用项已对齐为：`1a / 2b / 4d / 5e`
- 规则链路已完成语法校验

这一块的直接结果，是湖南专项已经从“编码定义存在”推进到“前端能录、规则能继承、提交能组合、云函数能按组合编码查表”的闭环状态。

### 6.2 湖南 2023 专项技术项目结构化修正

今天继续对湖南 2023 体育专项技术项目做人工复核和定向重建，不是简单重跑脚本，而是结合 PDF 原文页码对问题表逐项修补，重点处理篮球、排球、足球几个前面已经发现有缺页或元数据异常的项目。

#### 篮球项目

- 完成“1 分钟自投自抢投篮”项目结构化提取
- 已分别生成男子、女子独立 JSON
- 修正篮球专项“往返运球单手低手投篮”两份关键文件：
  - 男子 `m_003`
  - 女子 `f_004`
- 补齐男子缺失的 PDF 第 454-456 页数据
- 补齐女子缺失的 PDF 第 459-460 页数据
- 修正头部 `max_value` 与 `min_value` 填写错误
- 两份重建后的数据总条数均修正为 `970` 条
- 进一步修正“表 32 助跑摸高”男女评分表：
  - 男子 `m_001`：成绩范围校正为 `3.10-2.81m`
  - 女子 `f_002`：成绩范围校正为 `2.75-2.46m`
- 对“1 分钟自投自抢投篮”结果再次清洗，剔除成绩为“—”的无效行，仅保留有效成绩分值对

#### 排球项目

- 将“助跑摸高”由混合表拆分为男女两个独立文件：
  - `m_001`
  - `f_002`
- 按用户确认规则重算完整评分表：
  - 第一区间：`1cm / 0.5分`
  - 第二区间：`1cm / 1分`

#### 足球项目

- 重建女子绕杆射门 `f_003`，补齐漏掉的第 475 页数据
- 重建男子绕杆射门 `m_002`，补齐漏掉的第 472 页数据
- 按用户提供的正确分值重写足球颠球 `o_001`
- 将守门员加试立定跳远 `o_005` 拆分为男女独立表：
  - 男子 `m_005`
  - 女子 `f_006`
- 删除原有混合文件，避免后续读取混乱
- 修正后的文件均已通过解析校验

### 6.3 湖南 2026 新规编码与归总修正

这部分主要解决两个问题：一是把 2026 新版标准结构化结果补齐业务 `code`，二是把“湖南总表”的归总范围纠正回来，避免误把单目录汇总当成全量总表。

本轮已经完成：

- 湖南 2026 新版标准 OCR 结构化导出结果的业务 `code` 补齐
- 已覆盖的关键项目编码包括：
  - 立定跳远：`1A005`
  - 篮球：`1aa002`
  - 足球：`1aa001`
  - 游泳：`2bb001`
- 已生成 3 份核心结果文件：
  - `all_tables_with_code.json`
  - `all_tables_with_code - 扁平查询.json`
  - `json分类索引.json`
- 相关逻辑已通过脚本固化，支持后续继续增量生成

#### 归总范围修正

- 已修正“湖南总表”生成范围错误的问题
- 明确归总结果必须同时包含：
  - 2023 老表
  - 2026 新规
- 已修正 `aggregate_hunan_2026_json.py` 的归总逻辑
- 已跳过误生成的中间汇总文件，避免最终结果混入错误来源
- 已重新生成包含 `66` 张表的完整归总结果

### 6.4 湖南 2023 游泳专项重建

游泳专项是今天最关键的一块数据修复，因为前面已经确认 `04-08` 目录下多份 JSON 存在截断、乱序、漏页问题，单纯靠已有 CSV 清洗已经无法保证总表质量，因此这轮改为回到 PDF 原文页段重新重建。

本轮重点完成：

- 已确认 `04-08` 目录下 8 份 JSON 存在截断、乱序、漏页问题
- 重点对 6 张核心游泳表进行了按 PDF 原文页段重建：
  - 自由泳
  - 仰泳
  - 蝶泳
- 重建时统一采用 `0.01 秒`步长，保证连续计分覆盖
- 已解决男子 100 米自由泳等文件缺首页、缺末页的问题
- 已更新 `rebuild_hunan_swim_json.py`，将本次修复逻辑脚本化固化

### 6.5 当前结果与待续事项

截至目前，7.22 这轮工作的直接结果可以概括为：

- 湖南专项规则文件与前端提交逻辑已对齐
- 湖南 2023 多个专项关键 JSON 已完成重建或修正
- 湖南 2026 新规编码结果已补齐并完成归总范围修正
- 游泳专项核心表修复逻辑已沉淀为可复用脚本

当前仍有 1 项待续事项：

- 基于最新游泳修正结果，重新生成并同步湖南总表 `all_tables_with_code.json`，确保总表吸收本轮游泳数据修复

---

## 7. 7.17 今日更新

今天这轮不是单点小修，而是同时推进了前端页面重构、规则层梳理、用户偏好能力、云函数联动以及湖南数据继续结构化这几条线。相比上一轮更偏“把主干搭起来”，今天更偏“把细节补全并把不稳定点压住”。

### 6.1 `tool_f` 页面继续拆分与稳定化

围绕 `miniprogram/pages/tool/tool_f/`，今天继续对页面职责进行拆分和收口，目的不是单纯把文件拆开，而是把“页面交互”“规则装配”“提交调用”三块彻底解耦，方便后面继续接省份、接规则、接偏好而不再把逻辑堆回一个大文件。

今天在这块具体做了下面几件事：

- 将原先集中在 `tool_f.js` 里的逻辑继续拆分为三层：
  - `tool_f.js`：保留页面入口、生命周期、Picker 交互和页面状态流转
  - `tool_f_rules.js`：负责省份规则读取、项目结构标准化、Picker 数据构建、编码相关基础处理
  - `tool_f_submit.js`：负责校验、参数整理、提交云函数、结果页跳转
- 明确 `addrule.js` 的架构定位，把它作为“总规则表/统一规则入口”的承接位置，后续用于承接规则码、总分规则、特殊情况等公共配置
- 梳理 `tool_f` 到规则层的引用链，避免继续出现“实际生效入口”和“计划中的统一入口”混用的情况
- 保持 `Page({})` 注册入口不变，避免小程序框架因为入口拆散后识别不到页面主模块
- 给拆分后的文件补了更清晰的职责注释，后续回看时能快速定位哪一层该放什么逻辑

今天还顺手解决了两个稳定性问题：

- 修复了规则层文件里的语法错误，问题点在 `buildSkillItems` 相关逻辑里，属于三元表达式括号不完整导致的 `Unexpected token`
- 处理了微信小程序对多点文件名识别不稳定的问题。原先类似 `tool_f.rules.js`、`tool_f.submit.js` 这种命名，在运行时容易触发 `module not defined`，所以最终统一改回下划线命名的 `tool_f_rules.js`、`tool_f_submit.js`

也就是说，今天这一块的结果不是“只是拆分完成”，而是已经把拆分后的可运行性、可维护性和命名兼容性一起补齐了。

当前对应的核心文件包括：

- `miniprogram/pages/tool/tool_f/tool_f.js`
- `miniprogram/pages/tool/tool_f/tool_f_rules.js`
- `miniprogram/pages/tool/tool_f/tool_f_submit.js`
- `miniprogram/pages/tool/tool_f/rule2/addrule.js`

### 6.2 用户偏好能力补齐

今天还围绕 `openid` 把 `tool_f` 页面的个人偏好能力补全了一轮。这部分的目标很明确：让用户每次进页面时，不需要反复重新选省份、性别、主项、专项、辅项，而是能直接回到自己上次的配置。

这一轮先做过“自动保存偏好”的版本，后面又按实际使用感受调整成“进入页面自动读取，但保存动作改为手动触发”，这样可以避免用户在频繁切换过程中把临时状态误存为默认配置。

最终落地后的能力包括：

- 进入页面自动读取历史偏好
- 支持手动保存默认偏好
- 支持清空个人偏好
- 偏好数据写入登录用户记录中统一管理

偏好保存的内容不是只记一个省份，而是尽量完整地保留页面选择链路，覆盖：

- 当前省份
- 当前性别
- 主项选择状态
- 专项选择状态
- 辅项选择状态
- 各级索引信息

这样做的原因是 `tool_f` 不是简单的单级下拉框，而是多级联动结构；如果只存最终文本，不存索引和层级状态，页面回填时很容易出现“看起来像恢复了，实际上内部状态没对齐”的问题。

这部分联动修改还包括云函数侧的偏好读写接口扩展：

- 在 `cloudfunctions/sport_tool/login/index.js` 中增加/完善了偏好相关 `action`
- 支持读取偏好
- 支持更新偏好
- 支持清空偏好
- 将偏好统一挂到登录用户文档的 `toolPreferences.tool_f` 结构下，后续别的工具页如果也要做“默认偏好”，可以复用同一路径设计

前端页面层也做了配套调整：

- 在 `tool_f.wxml` 中增加“保存为默认偏好”和“清空个人偏好”按钮
- 在 `tool_f.wxss` 中补对应样式
- 在页面 `data` 中增加类似 `savingPreference` 一类状态位，用于给按钮和交互反馈做节流与 loading 控制

对应联动修改了：

- `cloudfunctions/sport_tool/login/index.js`
- `miniprogram/pages/tool/tool_f/tool_f.wxml`
- `miniprogram/pages/tool/tool_f/tool_f.wxss`

### 6.3 湖南数据继续修正

湖南数据这边，今天并不是简单“又跑了一次 OCR”，而是继续围绕已经抽出来的结构化结果做人工复核、脚本修正和编码整理，尤其把注意力放在游泳相关表、2026 新版标准页面抓取结果，以及结构化导出链路上。

重点推进的内容主要有三类：

1. 游泳相关数据继续修正

- 对游泳辅项和专项相关的 CSV / JSON 做继续复核
- 同步修正结构化导出目录中的表数据，保证 CSV 与 JSON 不是各改各的
- 补充游泳相关检查结果文件，方便后续快速定位哪些表已经核过、哪些地方还要继续追

2. 湖南 2026 新版标准继续落地为结构化产物

- 新增了一批 `结构化导出_2026` 目录下的转换结果
- 包括按项目拆分后的 CSV / JSON
- 包括中间过程图片
- 包括运行日志、汇总结果和过程记录文件

今天这批内容已经覆盖到例如：

- 身体素质中的立定跳远
- 辅助技术中的篮球辅项、足球辅项、游泳辅项

这些产物的意义不只是“有文件”，而是说明 2026 新版标准已经开始从网页/截图素材逐步落到统一的数据目录结构中，后续再接编码、再转数据库会顺很多。

3. 补充辅助脚本和编码规则文件

- 新增/维护了一批湖南数据处理用的临时或辅助脚本，用于批量 OCR、抽查、页数统计和专项修正
- 继续维护湖南数据编码与规则映射文件
- 增加了游泳相关修正脚本，方便对问题表做定向修补而不是整批重跑

这部分虽然看起来“文件很多很碎”，但实际上是在给后面的大批量转库做准备：先把 OCR 结果、结构化结果、问题记录、修正脚本、编码规则这几层都沉淀下来，后续不管是追 bug、重跑单项、还是对接数据库，都不用从头找材料

- 游泳辅项与专项的 CSV / JSON 同步修正
- 新增辅助校验脚本和批处理脚本，提升后续复查效率
- 持续维护湖南数据编码与规则映射文件

这部分改动主要位于：

- `shuju&guize/湖南/OCR可编辑分章Word/结构化导出/csv/`
- `shuju&guize/湖南/OCR可编辑分章Word/结构化导出/json/`
- `shuju&guize/湖南/OCR可编辑分章Word/结构化导出/json/_工具脚本/`
- `shuju&guize/湖南/数据编码和规则.json`

### 6.4 今天这一轮的直接结果

如果把今天的工作收拢成结果，可以概括为：

- `tool_f` 页面从“能跑”进一步推进到“结构更清楚、命名更稳、后续更好接功能”
- `tool_f` 已具备可用的个人偏好读取、保存和清空能力
- 登录云函数已经承担起 `tool_f` 偏好管理的后端入口职责
- 湖南数据目录继续从“原始材料堆积”向“结构化产物 + 修正脚本 + 编码规则”过渡
- README 里对今天工作的记录也同步补齐，后面回看不会只剩一句模糊描述

---

## 8. 当前目录中与本轮更新最相关的文件

### 小程序端

- `miniprogram/app.js`
- `miniprogram/utils/globalSubmit.js`
- `miniprogram/pages/tool/tool_f/tool_f.js`
- `miniprogram/pages/tool/tool_f/tool_f.wxml`
- `miniprogram/pages/tool/tool_f/rule2/provinceMap.js`
- `miniprogram/pages/tool/tool_f/rule2/distinguish_provinces_rule/index.js`
- `miniprogram/pages/tool/tool_f/rule2/distinguish_provinces_rule/001rule.js`
- `miniprogram/pages/tool/tool_f/rule2/distinguish_provinces_rule/002rule.js`

### 云函数端

- `cloudfunctions/sport_tool/login/index.js`
- `cloudfunctions/sport_tool/sport_tool_fun1/index.js`

---

## 9. 历史更新日志

- 7.22 完成湖南专项规则编码链路修正、2023 多个专项 JSON 重建、2026 新规编码归总修正，以及 2023 游泳专项核心表按 PDF 页段重建。
- 7.14 粗提取湖南 pdf-to-json。
- 7.11 处理 Git 仓库嵌套问题，删除外层 `sport/.git`，将 `dm/.git` 作为主仓库并使用 `master` 分支。
- 7.11 补充本地 Git 操作流程说明。
- 7.10 完成第一个页面 `index` 的 UI 设计。
- 6.30 `1.0.0` 初版：完成广东省份查分（游泳除外，健美操支持负数计算）。
