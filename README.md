# sport_tool
代码托管

## 更新日志

- 7.11 更新 git 仓库嵌套问题，删除了外层 `sport/.git`，将 `dm/.git` 设置为 `master` 分支。
- 7.11 补充说明，主包已明确本地 Git 的操作流程：先通过 `git add` 将本地仓库内容加入暂存区，再执行 `git commit` 提交。
- 7.10 更新，仅完成第一个页面 `index` 的 UI 设计。
- 6.30 更新日志，版本号 `1.0.0`。本项目围绕 `tool` 展开，旨在搭建工具使用平台。后续更新将以每新增一个工具组作为一个大版本。本次实现：
  1. 广东省份的查分（游泳除外，健美操支持负数计算）
  2. 暂未加入多余操作（如用户登录、历史记录等），这些将在后续推出
